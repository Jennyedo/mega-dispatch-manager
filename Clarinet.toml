[project]
name = "mega-dispatch-manager"
authors = ["Decentralized Dispatch Team"]
description = "A role-based task routing and tracking system on Stacks blockchain"
telemetry = false
requirements = []
[contracts.dispatch-manager]
path = "contracts/dispatch-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
